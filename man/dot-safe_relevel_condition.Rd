% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_core.R
\name{.safe_relevel_condition}
\alias{.safe_relevel_condition}
\title{Safely relevel a condition factor to a specified base/reference level}
\usage{
.safe_relevel_condition(x, base, label = "condition")
}
\arguments{
\item{x}{a factor (or character vector, which will be coerced) to relevel}

\item{base}{the level that should become the reference; NULL/"" skips releveling}

\item{label}{a short name for the factor/column, used in messages only}
}
\value{
the releveled factor, or `x` unchanged (as a factor) if releveling was skipped
}
\description{
Centralizes the guard that used to live inline (and inconsistently) in
both create_dds_object() and run_dte(): skips releveling -- with a
message that says *why* -- instead of either (a) silently doing nothing
while claiming "not provided" when a value actually was supplied but
just didn't apply to this call, or (b) crashing with relevel()'s opaque
"'ref' must be an existing level" error when `base` doesn't match any
actual level of the factor (e.g. mismatched default level/base values
for this dataset's condition labels).
}
\keyword{internal}
