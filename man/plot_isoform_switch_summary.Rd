% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mod_isoform.R
\name{plot_isoform_switch_summary}
\alias{plot_isoform_switch_summary}
\title{Generate Isoform Switch Summary Plots (structure + expression + usage)}
\usage{
plot_isoform_switch_summary(
  switch_list,
  plot_dir,
  genes_of_interest = NULL,
  level = NULL,
  base = NULL,
  top_n = 10,
  alpha = 0.05,
  dIFcutoff = 0.1
)
}
\arguments{
\item{switch_list}{Object returned by run_isoform_switch()}

\item{plot_dir}{Directory to save plots into (created if missing)}

\item{genes_of_interest}{Optional character vector of gene symbols to
force-include regardless of significance}

\item{level}{Foreground condition label (used to resolve condition2 for
forced gene plots; if NULL, taken from switch_list)}

\item{base}{Reference condition label (used to resolve condition1 for
forced gene plots; if NULL, taken from switch_list)}

\item{top_n}{Number of top switching genes to auto-plot (default 10)}

\item{alpha}{Significance cutoff on the isoform switch q-value (default 0.05)}

\item{dIFcutoff}{Minimum |dIF| for a switch to be considered (default 0.1)}
}
\value{
Invisibly, a character vector of generated PDF paths
}
\description{
Wraps \code{IsoformSwitchAnalyzeR::switchPlotTopSwitches()} to automatically
render the top-N most significant isoform switches, and additionally forces
individual \code{switchPlot()} figures for any explicitly requested genes
(even if not among the automatically selected top switches). Each figure is
a composite panel showing (1) isoform/transcript structure with any
annotated ORF, coding potential, protein domains, or signal peptides,
(2) gene expression, (3) isoform expression, and (4) isoform usage (IF),
including the switch-test result.
}
