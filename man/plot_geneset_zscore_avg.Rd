% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mod_plots.R
\name{plot_geneset_zscore_avg}
\alias{plot_geneset_zscore_avg}
\title{Plot Average Z-Score by Condition for Gene Set(s)}
\usage{
plot_geneset_zscore_avg(
  dds,
  gene_sets,
  condition_col,
  level,
  base,
  plot_dir,
  include_global = TRUE,
  set_name = NULL
)
}
\arguments{
\item{dds}{A DESeqDataSet object (rownames should already be gene symbols)}

\item{gene_sets}{Named list of character vectors, e.g.
\code{list(Tightness = c("Cdh5","Pdgfa"), "Lipid Scavengers" = c("Cd36","Stab1"))}.
List names are used as facet/panel titles unless `set_name` is provided.}

\item{condition_col}{Character string representing the design metadata column}

\item{level}{String representing the foreground group}

\item{base}{String representing the background reference group}

\item{plot_dir}{String directory path where the PDF will be saved}

\item{include_global}{Logical: append a pooled "Global (All Genes)" panel
combining every gene across all supplied sets (default TRUE, ignored if
`set_name` is not NULL)}

\item{set_name}{Optional character string. If provided, `gene_sets` must be a
list of length 1; this name is used in the output filename and plot title,
and faceting is suppressed.}
}
\value{
Invisibly returns the ggplot object
}
\description{
Computes a per‑gene z‑score across samples, then averages the z‑scores across
all genes within each gene set for every sample, producing one "module score"
per sample per set. If `set_name` is supplied, only one set is expected and
the plot is saved as a single PDF (without faceting) using that name in the
title and filename.
}
